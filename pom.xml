<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        Inherit from Spring Boot Parent.
        This gives us all the default plugin configurations and dependency versions
        for Spring Boot 3.3.0.
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.0</version>
        <relativePath/>
    </parent>

    <!-- ======== PROJECT METADATA ======== -->
    <groupId>com.tradejournal</groupId>
    <artifactId>trade-journal-platform</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>trade-journal-platform</name>
    <description>Microservices platform for Trade Journal &amp; Analytics</description>

    <!-- ======== PROPERTIES (Version management) ======== -->
    <properties>
        <java.version>17</java.version>

        <!-- Spring Cloud (for API Gateway and Service Discovery) -->
        <spring-cloud.version>2023.0.1</spring-cloud.version>

        <!-- JWT -->
        <jjwt.version>0.12.5</jjwt.version>

        <!-- Jackson (for JSON serialization) -->
        <jackson.version>2.17.1</jackson.version>

        <!-- Testing -->
        <testcontainers.version>1.19.3</testcontainers.version>

        <!-- Maven Plugins -->
        <maven-compiler-plugin.version>3.11.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.2.5</maven-surefire-plugin.version>

        <flyway.version>10.20.1</flyway.version>
    </properties>

    <!-- ======== CHILD MODULES ======== -->
    <modules>
        <module>common-library</module>
        <module>user-service</module>
        <module>trade-service</module>
        <module>analytics-service</module>
        <module>integration-service</module>
        <module>api-gateway</module>
        <!-- Optional: Add discovery-service later if you want Eureka -->
        <!-- <module>discovery-service</module> -->
    </modules>

    <!-- ======== DEPENDENCY MANAGEMENT ======== -->
    <!--
        This section defines versions for all dependencies used across modules.
        Child modules only need to declare the <groupId> and <artifactId>
        (they inherit the version from here).
    -->
    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud Dependencies (BOM) -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Common Library (shared DTOs and events) -->
            <dependency>
                <groupId>com.tradejournal</groupId>
                <artifactId>common-library</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- JWT Dependencies -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- Lombok (version is already managed by Spring Boot parent, but we override it here) -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <optional>true</optional>
            </dependency>

            <!-- Testcontainers (for integration testing) -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>postgresql</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>kafka</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ======== BUILD CONFIGURATION ======== -->
    <!--
        Common plugin configurations for all child modules.
        Child modules can override these if needed.
    -->
    <build>
        <pluginManagement>
            <plugins>
                <!-- Spring Boot Maven Plugin (repackages the JAR) -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>

                <!-- Maven Compiler Plugin (enforces Java 17) -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <!--suppress UnresolvedMavenProperty -->
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>

                <!-- Maven Surefire Plugin (for running JUnit tests) -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                    <configuration>
                        <includes>
                            <include>**/*Test.java</include>
                            <include>**/*Tests.java</include>
                        </includes>
                        <argLine>-XX:+EnableDynamicAgentLoading</argLine>
                    </configuration>
                </plugin>

                <!-- Dockerfile Maven Plugin (for building Docker images) -->
                <!-- Optional: Uncomment if you want to build Docker images with Maven -->
                <!--
                <plugin>
                    <groupId>com.spotify</groupId>
                    <artifactId>dockerfile-maven-plugin</artifactId>
                    <version>1.4.13</version>
                </plugin>
                -->
            </plugins>
        </pluginManagement>
    </build>

    <!-- ======== REPOSITORIES ======== -->
    <!--
        Ensures Maven can fetch dependencies from the central repository.
        You can add custom repositories here if needed.
    -->
    <repositories>
        <repository>
            <id>central</id>
            <name>Central Repository</name>
            <url>https://repo.maven.apache.org/maven2</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

</project>